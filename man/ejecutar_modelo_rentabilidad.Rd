% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rentabilidad.R
\name{ejecutar_modelo_rentabilidad}
\alias{ejecutar_modelo_rentabilidad}
\title{Ejecuta el modelo completo de rentabilidad electoral}
\usage{
ejecutar_modelo_rentabilidad(
  obj_eleccion,
  eleccion = "pm",
  nivel_geo = "seccion",
  coaliciones,
  params,
  anios = NULL
)
}
\arguments{
\item{obj_eleccion}{Objeto `Tablero` o `Electoral`.}

\item{eleccion}{Tipo de elección: `"pm"`, `"dl"`, `"df"`, `"pr"`, `"sen"`, `"gb"`.}

\item{nivel_geo}{Nivel de agregación: `"seccion"` (default), `"municipio"`, `"dl"`, `"df"`.}

\item{coaliciones}{Lista nombrada por año (e.g. `"2018"`, `"2021"`, `"2024"`), donde
cada elemento tiene `$morena`, `$opo1`, y opcionalmente `$opo2`.}

\item{params}{Lista de parámetros. Usa [params_rentabilidad_default()] como punto de partida.}

\item{anios}{Vector entero de exactamente 3 años a usar. Si es `NULL` (default), se
detectan automáticamente y se toman los 3 más recientes disponibles en el objeto.}
}
\value{
Lista con dos elementos:
  \describe{
    \item{`panel_largo`}{Panel seccional/municipal por año con porcentajes y márgenes.}
    \item{`resumen`}{Tabla con cuadrantes, clases A/B, scores SA/SF e IR.}
  }
}
\description{
A partir de un objeto `Tablero`/`Electoral`, una lista de coaliciones por año
y un conjunto de parámetros, construye el panel largo de secciones/municipios
y calcula el Índice de Rentabilidad (IR) con su clasificación completa.
}
\examples{
\dontrun{
params <- params_rentabilidad_default()
coaliciones <- list(
  "2018" = list(morena = c("morena", "pt", "pes"), opo1 = c("pan", "prd")),
  "2021" = list(morena = c("morena"), opo1 = c("pan", "pri", "prd")),
  "2024" = list(morena = c("morena", "pt", "pvem"), opo1 = c("pan", "pri", "prd"))
)
resultado <- ejecutar_modelo_rentabilidad(mi_tablero, "pm", "seccion",
                                          coaliciones, params)
resultado$resumen
}
}
