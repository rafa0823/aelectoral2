% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rentabilidad.R
\name{optimizar_params}
\alias{optimizar_params}
\title{Optimiza los parámetros del modelo de rentabilidad}
\usage{
optimizar_params(
  resumen_df,
  objetivo_fn = objetivo_rentabilidad,
  params_iniciales = params_rentabilidad_default(),
  n_starts = 10L,
  objetivo_cobertura_A = 0.4,
  escala_umbral = 0.02,
  params_fijos = list(momentum_min_alza = 0, delta_comoda_min = 0.03, delta_fuerte_min =
    0.05),
  seed = 42L,
  ...
)
}
\arguments{
\item{resumen_df}{Data frame con el resumen de desempeño (salida del paso
interno de [ejecutar_modelo_rentabilidad()], o construido manualmente).}

\item{objetivo_fn}{Función objetivo a minimizar. Debe aceptar `(par, resumen_df, ...)`.
Por default usa [objetivo_rentabilidad()].}

\item{params_iniciales}{Lista de params para el punto de inicio principal.
Default: [params_rentabilidad_default()].}

\item{n_starts}{Número de puntos de inicio aleatorios adicionales. Default `10`.}

\item{objetivo_cobertura_A}{Pasado a `objetivo_fn`. Default `0.40`.}

\item{escala_umbral}{Pasado a [vec_a_params()] y [params_a_vec()]. Default `0.02`.}

\item{params_fijos}{Params que no se optimizan (momentum, deltas). Default estándar.}

\item{seed}{Semilla para reproducibilidad de puntos aleatorios. Default `42`.}

\item{...}{Argumentos adicionales pasados a `objetivo_fn`.}
}
\value{
Lista con:
  \describe{
    \item{`params`}{Lista de params óptimos, compatible con [clasificar_rentabilidad()].}
    \item{`valor`}{Valor de la función objetivo en el óptimo.}
    \item{`convergencia`}{Código de convergencia de `optim()`.}
    \item{`todos_starts`}{Data frame con resultados de todos los puntos de inicio.}
  }
}
\description{
Busca la combinación de umbrales y pesos que minimiza la función objetivo
sobre el `resumen_df` dado. Usa `stats::optim()` con múltiples puntos de
inicio para reducir la dependencia de óptimos locales.
}
\examples{
\dontrun{
# resumen_df viene del paso intermedio del modelo
res <- ejecutar_modelo_rentabilidad(mi_tablero, "pm", "seccion",
                                    coaliciones, params_rentabilidad_default())
opt <- optimizar_params(res$resumen, objetivo_cobertura_A = 0.35)
opt$params   # params optimizados

# Aplicar params optimizados
resumen_opt <- clasificar_rentabilidad(res$resumen, opt$params)
}
}
