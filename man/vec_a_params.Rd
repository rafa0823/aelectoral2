% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rentabilidad.R
\name{vec_a_params}
\alias{vec_a_params}
\title{Convierte un vector libre (16 valores) en una lista de params válida}
\usage{
vec_a_params(
  par,
  escala_umbral = 0.02,
  params_fijos = list(momentum_min_alza = 0, delta_comoda_min = 0.03, delta_fuerte_min =
    0.05)
)
}
\arguments{
\item{par}{Vector numérico de longitud 16 (escala no restringida).}

\item{escala_umbral}{Factor de escala para los umbrales. Default `0.02`.}

\item{params_fijos}{Lista de params que no se optimizan (momentum, deltas).}
}
\value{
Lista de params compatible con [clasificar_rentabilidad()].
}
\description{
Aplica las transformaciones necesarias para garantizar que los umbrales
queden ordenados y los pesos sumen 1. Útil para inspeccionar qué params
corresponden a un vector dado durante la optimización.

El vector tiene esta estructura:
`par[1:3]`  — log-diferencias para umbrales positivos (retencion, ventaja, bastion)
`par[4:6]`  — log-diferencias para umbrales negativos (flip inmediato, probable, paquete)
`par[7:10]` — pesos SA (softmax)
`par[11:14]`— pesos SF (softmax)
`par[15:16]`— mezcla IR (softmax)
}
